export const selectCampers = (state) => state.campers.adverts;

export const selectFavoriteCampers = (state) => state.favorites;
export const selectIsLoading = (state) => state.campers.isLoading;
export const selectError = (state) => state.campers.error;